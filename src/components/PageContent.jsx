import React from 'react'

const PageContent = ({children}) => {
  return (
    <div style={{paddingTop: '60px', maxWidth: '1400px', margin: 'auto'}}>{children}</div>
  )
}

export default PageContent